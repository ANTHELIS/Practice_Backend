<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.7.0/fonts/remixicon.css"rel="stylesheet"/>
</head>
<body>

    <div class="w-screen h-screen bg-zinc-900 text-white p-8">
        <div class="flex gap-3 items-center">
            <div class="w-14 h-14 rounded-2xl">
                <img class="w-full h-full object-cover rounded-2xl" src="/images/uploads/<%=user.profilepic%>" alt="">
            </div>
            <h1 class="text-2xl">Hello, <%= user.name %> 👋</h1>
        </div>
        <h3 class="mb-6">You can create a new post</h3>
        <div class="new_post text-white mb-20">
            <form action="/post" method="post">
                <textarea class="block w-1/3 px-3 py-1 bg-transparent outline-none border-2 border-zinc-600 rounded-md resize-none h-30 " name="content" placeholder="What's on your mind ?"></textarea>
                <input class="w-fit px-3 py-1 mt-3 cursor-pointer rounded-md bg-blue-900" type="submit" value="Create New Post">
            </form>
        </div>

        <% if(user.posts.length <= 0){ %>
        <h2 class="text-zinc-400 mb-4">Your have not post yet</h2>
        <% } else { %>
        <h2 class="text-zinc-400 mb-4">Your Posts</h2>
        <div class="posts text-white rounded-lg w-full p-1 flex flex-wrap gap-3">
                <% user.posts.reverse().forEach((post)=>{ %>
                    <div class="post w-1/3 h-fit bg-zinc-700 rounded-md p-3 tracking-tight flex flex-col gap-1">
                        <div class="flex justify-between">
                            <h3 class="text-blue-400">@<%= user.username %></h3>
                            <div class=" flex gap-1">
                                <a href="/edit/<%= post._id %>"><i class="ri-edit-fill text-teal-400  px-2 py-1 hover:bg-zinc-600 rounded-md "></i></a>
                                <a href="/delete/<%= post._id %>"><i class="ri-delete-bin-2-fill text-red-400 px-2 py-1 hover:bg-zinc-600 rounded-md "></i></a>
                            </div>
                        </div>
                        <h3  class="mb-2"><%= post.content %></h3>
                        <a class="w-fit text-sky-500" href="/like/<%= post._id %>"> <%= post.likes.length %> 
                            <% if(post.likes.indexOf(user._id) === -1){ %>
                            <i class="ri-thumb-up-line text-blue-500 p-1 hover:bg-zinc-600 rounded-md w-fit ml-1"></i>
                            <% } else { %>
                            <i class="ri-thumb-up-fill text-blue-500 p-1 hover:bg-zinc-600 rounded-md w-fit ml-1"></i> 
                            <% } %>
                        </a>
                    </div>
                <% }) %>
    
    
        </div>
        <% } %>



    </div>


</body>
</html>